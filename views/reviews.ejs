<%- include('partials/header') %>

<h1 class="title">📖 Recensioner</h1>

<% if (user) { %>
<form method="POST" action="/reviews" class="box">
  <div class="field">
    <label class="label">Din recension som <%= user.username %></label>
    <div class="control">
      <textarea class="textarea" name="message" required></textarea>
    </div>
  </div>
  <div class="control">
    <button type="submit" class="button is-success">Skicka</button>
  </div>
</form>
<% } else { %>
  <p><a href="/users/login">Logga in</a> för att skriva en recension.</p>
<% } %>

<hr>

<h2 class="subtitle">Tidigare recensioner:</h2>

<% if (reviews.length === 0) { %>
  <p>Inga recensioner än.</p>
<% } else { %>
  <% reviews.slice().reverse().forEach(r => { %>
    <div class="box">
      <strong><%= r.name %></strong> (<%= r.date %>)<br/>
      <p><%= r.message %></p>
			<% if (user && user.username === r.name) { %>
  <a href="/reviews/edit/<%= reviews.length - 1 - i %>" class="button is-small is-warning">Redigera</a>
<% } %>
    </div>
  <% }) %>
<% } %>

<%- include('partials/footer') %>
