<%- include('partials/header') %>
	<h1 class="title">🛠 Adminpanel</h1>

	<% if (reviews.length===0) { %>
		<p>Inga recensioner ännu.</p>
		<% } else { %>
			<ul>
				<% reviews.forEach((r, index)=> { %>
					<li class="box">
						<strong>
							<%= r.name %>
						</strong> (<%= r.date %>)<br>
							<p>
								<%= r.message %>
							</p>
							<p>Status:
								<% if (r.approved) { %>
									✅ Godkänd
									<% } else { %>
										⏳ Väntar
										<form method="POST" action="/admin/approve/<%= index %>" style="display:inline;">
											<button class="button is-small is-success">Godkänn</button>
										</form>
										<% } %>
							</p>
							<form method="POST" action="/admin/delete/<%= index %>">
								<button class="button is-small is-danger">Ta bort</button>
							</form>
					</li>
					<% }) %>
			</ul>
			<% } %>

				<%- include('partials/footer') %>