<!DOCTYPE html>
<html lang="sv">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Trädgårdscafé</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@1.0.0/css/bulma.min.css">
  <link rel="stylesheet" href="/styles.css">
</head>

<body>
  <nav class="navbar is-fixed-top is-primary" role="navigation">
    <div class="navbar-brand">
      <a class="navbar-item" href="/">🌿 Trädgårdscafé</a>
    </div>
    <div class="navbar-menu">
      <div class="navbar-start">
        <a class="navbar-item" href="/">Hem</a>
        <a class="navbar-item" href="/menu">Meny</a>
        <a class="navbar-item" href="/contact">Kontakt</a>
        <a class="navbar-item" href="/reviews">Recensioner</a>
        <div class="navbar-end">
          <% if (typeof user !=='undefined' && user) { %>
            <div class="navbar-item">
              Inloggad som <strong>
                <%= user.username %>
              </strong>
            </div>
            <div class="navbar-item">
              <a href="/users/logout" class="button is-light">Logga ut</a>
            </div>
            <% } else { %>
              <div class="navbar-item">
                <div class="buttons">
                  <a href="/users/register" class="button is-primary">Registrera</a>
                  <a href="/users/login" class="button is-light">Logga in</a>
                </div>
              </div>
              <% } %>
        </div>

      </div>
    </div>
  </nav>
  <section class="section">
    <div class="container">